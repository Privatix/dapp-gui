import * as React from 'react';
import ChannelToolClose from './channelToolClose';

export default function(props:any){

    return <div> channel tools component: <ChannelToolClose channelId={props.channel.id} /></div>;

}
